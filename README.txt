# Parte 1: 
# Calcular el salario neto de los empleados aplicando un bono de 500.00
# si pertenecen al departamento de "Ventas" Y tienen más de 5 años de antigüedad, o si superaron su meta de rendimiento.
# Al final, se debe guardar un reporte en un nuevo CSV.

# Parte 2: 
# Definir métodos adicionales para solucionar el problema de forma más limpia:
# Método_1 de encapsulamiento en la clase (Empleado.calcular_neto): En lugar de hacer la lógica matemática en una función externa del módulo procesador.py, 
# ¿podemos mover la lógica de la condición y la asignación directamente como un método dentro de la clase Empleado?
# Método_2 de validación de datos (Empleado.validar_datos): Un método que verifique que la antigüedad y el salario no sean negativos antes de realizar los cálculos para evitar errores con datos corruptos del CSV.

